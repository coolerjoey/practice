#pragma once

#include <vector>

using namespace std;

class MYDynamicProgramming
{
private:
    /* data */
public:
    MYDynamicProgramming(/* args */);
    ~MYDynamicProgramming();

    static int cuttingRope(int n);
    static bool isMatch(string s, string p);
};

